#include <stdio.h>
#include <string.h>
int main(){
  char str [100] ;
  gets(str);
  int count=0;
  for(int i=0; str[i]!='\0';i++){
      count++;
  }
  printf("%d\n",count);
  for(int i=count-1; i>=0; i--){
     printf("%c",str[i]);
  }


return 0;
}

